## Summary

Applied review feedback to PR #18 (dependabot npm dependency bumps, genkit 1.28 -> 1.36.0).

### Changes Made

1. **Updated stale comment in `unified-adapter.ts` (line 187)**
   - Changed `@genkit-ai/googleai@1.28` to `@genkit-ai/googleai@1.36` in the comment explaining `modelVersion` resolution

2. **Updated stale comment in `factory.ts` (line 154)**
   - Same version reference updated from `@genkit-ai/googleai@1.28` to `@genkit-ai/googleai@1.36` in the `modelVersion` JSDoc

### Verification

All quality gates passed: typecheck, lint, format:check, tests.
