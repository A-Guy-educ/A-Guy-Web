{
  "name": "infra",
  "description": "Monitors site availability, API latency, console errors, and CI health",
  "domain": "infra",
  "labels": ["kody:watch:infra"],
  "cron": "0 8 * * *",
  "notify": {
    "channels": ["slack"],
    "color": "danger",
    "when": "on-failure"
  }
}
