Added `@ai-summary` (with `@fileType`, `@domain`, `@pattern`) to 20 source files across `src/infra/utils/` and its subdirectories (`logger/`, `test/`, `validation/`). Test files were excluded (`.test.ts`). The original body content of `normalize-latex.ts` was removed as orphaned comment — it was purely explanatory prose about remark-math, not needed for the `@ai-summary` header. One edit round was needed after a typecheck failure in normalize-latex.ts caused by the orphaned comment.
