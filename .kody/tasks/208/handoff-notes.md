Resolved 8 asymmetric merge conflicts (delete/update) in `.kody/duties/` and `.kody/executables/` files. The conflicts arose because dev added new duty/executable infrastructure files (preview-health, task-leader, vercel-production-deploy) while the PR branch had simplified versions of the same files already in the working tree. Resolved by accepting the HEAD (PR branch) version for all 8 files via `git add`. PR is focused on doc-coverage for src/lib/payment/ — the .kody infrastructure files are peripheral, so keeping the PR's simplified versions preserves PR intent without affecting the doc-coverage goal.