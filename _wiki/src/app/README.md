# App Routes

This folder contains the public Next.js App Router pages and API routes.

- `(frontend)/` holds the web experience.
- `api/` holds plain Next.js route handlers.
- CMS/admin routes have been removed.
