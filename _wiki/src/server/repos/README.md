# Server Repositories

This folder contains small data access helpers used by the public web app.

The current app has no bundled CMS data source, so these helpers return empty or static defaults until a new backend is connected.
