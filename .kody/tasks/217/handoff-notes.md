Resolved the single conflict in `src/infra/loading/utils/resolveHref.ts`. The conflict was asymmetric — both sides had identical function code but conflicting JSDoc block formatting. HEAD split the @ai-summary into multiple blocks with additional @fileType/@domain annotations; origin/dev had a single consolidated @ai-summary. Took origin/dev's consolidated comment as it was cleaner and the actual code was byte-for-byte identical. Typecheck and lint pass cleanly.
