/**
 * Grant product entitlements after successful payment.
 *
 * @ai-summary Stub: does nothing yet — entitlements must be wired to the actual
 *              entitlement system before checkout completions are trusted to gate access.
 */

export async function grantProductEntitlements(
  _userId: string,
  _productId: string,
  _transactionId: string,
): Promise<void> {}
