Resolved merge conflicts in `src/lib/payment/paypal.ts` and `src/lib/payment/stripe.ts`. Both conflicts were symmetric — only the JSDoc file header differed between HEAD (PR branch adding doc headers) and origin/dev (simpler description). Took HEAD side since this PR (#207) is specifically a doc-coverage pass adding @fileType, @domain, and @ai-summary headers to payment provider files. Typecheck passes after resolution.
