# Preference: Do Not Alter GitHub Workflow YAML

The operator clarified that Codex/Kody should not add, edit, or delete GitHub workflow `.yml` files under `.github/workflows/` unless they explicitly approve that action.

Context: This was clarified after an unapproved attempt to add a Vercel preview workflow. Treat questions about workflow strategy as discussion only until the operator explicitly asks for implementation.
